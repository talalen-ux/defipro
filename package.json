{
  "name": "meridian-protocol",
  "version": "0.1.0",
  "description": "Meridian: an on-chain repo market for tokenized assets, publishing the MOR benchmark rate",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "demo": "hardhat run scripts/demo.js",
    "deploy": "hardhat run scripts/deploy.js"
  },
  "license": "BUSL-1.1",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6",
    "solc": "0.8.24"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
